//
//  Colors.swift
//  PTV
//
//  Created by Wael Saad on 23/5/2022.
//

import SwiftUI

extension Color {
    static let accentColor = Color("AccentColor")
    static let navy = Color("navy")

    static let secondaryText = Color("secondaryTextColour")
    static let navigationBarBackground = Color("navigationBarBackgroundColour")
    
    static let navyInLightTealInDark = Color("navyInLightTealInDarkColour")

    static let splashImage = Color("splashImageColour")
    
    static let primaryButton = Color("primaryButtonColour")

    static let boxedImageBackground = Color("boxedImageBackgroundColour")

    static let pageControlDot = Color("pageControlDot")

    static let navLinkText = Color("navLinkTextColour")
    static let navLinkBackground = Color("navLinkBackgroundColour")

    static let progressSpinner = Color("progressSpinnerColour")

    static let foreground = Color("foregroundColour")

    static let infoCardBackground = Color("infoCardBackgroundColour")
    static let infoCardBackgroundInSheet = Color("infoCardBackgroundInSheetColour")
    static let cardContainerBackground = Color("cardContainerBackgroundColour")

    static let cardBackground = Color("cardBackgroundColour")

    static let cardShadow = Color("cardShadowColour")
    static let cardBackgroundInSheet = Color("cardBackgroundInSheetColour")

    static let progressOverlayBackground = Color("progressOverlayBackgroundColour")

}
